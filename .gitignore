# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# IDE / Cursor — keep project rules in git so cloud agents see them
.cursor/*
!.cursor/rules/
!.cursor/rules/**

.vscode/
*.code-workspace

# Secrets — never commit these
.env
.env.*
*.pem
*.key
credentials.json
secrets.json

# Logs / temp
*.log
npm-debug.log*
.cache/
tmp/
temp/

# Dependencies (if you add tooling later)
node_modules/

# Build output (if added later)
dist/
build/
